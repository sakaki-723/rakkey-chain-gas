# ラッkey chain展 GASシステム 引き継ぎ書

## 概要

「ラッkey chain展」（第5回）のアートグループ展において、複数作家のキーチェーンを委託販売するイベントの在庫・ロイヤリティ管理システム。Google Sheets + Google Apps Script（GAS）で構築されており、Square POS連携を含む。

現在のファイルは `gas_script.gs`（引き継ぎ書と同フォルダに同梱）。GASエディタで丸ごと貼り替えて使う運用。

---

## スプレッドシート構成

### インプット系シート（手動入力・フォーム受信）

| シート名 | 内容 |
|---|---|
| `ラッKey chain5納品予定回答` | 納品予定Googleフォームの回答が自動入力される |
| `ラッKey chain5納品確定回答` | 納品確定Googleフォームの回答が自動入力される |
| `在庫変動ログ` | 納品・サンプル・取り置き・取り置き解消・破損・返却・買取済在庫・紛失・繰越入庫・振替出庫を手動記録する。**「販売」種別は廃止**（Squareデータで管理するため）。G列に担当者列あり（集計には未使用） |
| `Square売上` | Squareからエクスポートした売上サマリCSVをコピペ。A列=商品名(作家名)、B列=商品バリエーション(作品名)、C列=商品番号(SKU)、M列=販売数、N列=払い戻し数 |

### マスタ系シート

| シート名 | 内容 |
|---|---|
| `作家マスタ` | 全作家の基本情報。A:作家番号、B:アーティスト名、C:ラベル表記名、D:instagram、E:ジャンル、F:メールアドレス、G:住所1（返送先）、H:住所2（未使用）、I:本名、J:電話番号、K:振込先情報、L:取引申請書送付済み、M:取引申請書受信済み（チェックボックス）、N:備考 |
| `会期ごと作家情報` | 全会期の作家参加状況を蓄積。A:会期（例：5、NEO）、B:作家番号、C:作家名（=XLOOKUP(B列, 作家マスタA:A, 作家マスタB:B, "")）、D:備考、E:担当者、F:依頼状況、G:見送り理由、H:納品状況、I:撮影サンプル、J:納品予定フォーム受取（チェックボックス）、K:納品確定フォーム受取（チェックボックス）、L:納品確認書送付、M:ロイヤリティレポート送付、N:請求書受取、O:振込 |
| `商品コード管理` | 作品ごとの価格・コード管理。A:作家名、B:ラベル表示作家名（作家マスタのC列「ラベル表記名」から自動反映）、C:作品名、D:税込価格（**②-2実行時に商品一覧_確定の値でミラー上書きされる。価格の唯一のソースは商品一覧_確定**）、E:ラベル表示商品名（手入力）、F:商品コード、G:在庫残数（**会期末に実数カウントした値を手入力する欄。在庫管理(商品ごと)の「残在庫(実数)」はここをミラーするだけで、スクリプト実行時に消えない**） |

### 中間生成シート（GASで自動生成・上書き禁止）

| シート名 | 内容 |
|---|---|
| `商品一覧_予定` | 納品予定フォーム回答を縦持ちに展開したもの |
| `商品一覧_確定` | 納品確定フォーム回答を縦持ちに展開したもの。作家マスタ未登録の行は作家名セルが薄い赤になる |

### 出力系シート（GASで自動生成）

| シート名 | 列構成 |
|---|---|
| `在庫管理(商品ごと)` | 作家名 / 作品名 / 商品コード / 税込価格（**商品一覧_確定から直接取得**）/ サンプル数 / 納品予定数 / 納品済み(サンプル込) / 取置き中 / 紛失 / 破損 / 返却（**J・K列はデフォルトで非表示。折りたたみを展開すれば見られる**）/ 在庫数（理論値、販売を引かない。買取済在庫＋繰越入庫を加算、振替出庫を減算） / 推定販売数（在庫数（理論値）－残在庫(実数)、手入力後に自動計算） / 残在庫(実数)（商品コード管理「在庫残数」のミラー、手入力欄は商品コード管理側） |
| `在庫管理(SKUごと)` | 作家名 / 作品名 / 商品コード / 税込価格 / サンプル数 / 納品予定数 / 納品済み(サンプル込) / 取置き中 / 紛失 / 破損 / 返却（**デフォルト非表示**）/ 在庫数（理論値） / 残在庫(実数) / 推定販売数（在庫管理(商品ごと)の推定販売数のSKU合算） / Square販売数（Square売上シートから作家名＋作品名で突き合わせてSKU合算）。不一致行は薄い赤に色変え |
| `納品確認書` | 作家ごとの納品確認書。H3に作家名プルダウン、C13に会期プルダウン |
| `ロイヤリティレポート` | C13に会期プルダウン。ここの値が「現在の会期」として各種処理に使われる |

---

## GAS関数一覧

### メニュー構成（📦 納品管理）

| メニュー項目 | 関数名 | 内容 |
|---|---|---|
| ① 納品予定を更新 | `updatePlanList()` | 納品予定フォーム回答→商品一覧_予定 |
| ②-1 商品一覧_確定のみ更新 | `updateFinalListOnly()` | 商品一覧_確定のみ（プルダウン・商品コードは触らない） |
| ②-2 納品確定を更新 | `updateFinalList()` | 商品一覧_確定＋納品確認書プルダウン＋商品コード管理も更新 |
| ③ 在庫管理(商品ごと)を更新 | `updateInventorySheet()` | 商品一覧_確定＋在庫変動ログ＋商品コード管理から在庫管理(商品ごと)を生成 |
| ③-2 在庫管理(SKUごと)を更新 | `updateInventorySheetBySku()` | 在庫管理(商品ごと)＋Square売上から在庫管理(SKUごと)を生成 |
| 納品確認書を手動更新 | `manualRefreshInvoice()` | 現在選択中の作家名で納品確認書を再生成 |
| 納品確認書プルダウンのみ再設定 | `setupInvoiceDropdown()` | 商品一覧_確定ベースで納品確認書のH3プルダウンを再設定 |
| 会期プルダウンを更新 | `setupRoyaltyPeriodDropdown()` | ロイヤリティレポート!C13・納品確認書!C13の両方に会期プルダウンを設定 |
| フォーム受取チェックを再実行（手動） | `recheckFormReceipts()` | フォーム回答シートを全件再スキャンし、会期ごと作家情報のJ/K列にチェックを入れ直す。作家マスタを後から修正した場合などに使う |
| 🔍 作家名で検索してNo.を入力 | `searchArtistAndFillNumber()` | 会期ごと作家情報のB列セルを選んで実行→作家名の一部を入力→候補から選択してNo.を入力 |

### 自動トリガー

| 関数名 | トリガー種別 | 内容 |
|---|---|---|
| `onOpen(e)` | 簡易トリガー | スプレッドシートを開いたときにメニューを生成 |
| `onEdit(e)` | 簡易トリガー | 作家マスタへの入力時に重複チェック、在庫変動ログへの入力時に商品名プルダウン更新・確認日自動入力 |
| `onFormSubmit(e)` | **インストーラブルトリガー（手動設定が必要）** | 納品予定・確定フォーム送信時に、ロイヤリティレポート!C13の現在の会期を参照して会期ごと作家情報のJ/K列にチェックを入れる |

> ⚠️ `onFormSubmit`は**GASエディタのトリガー画面から手動でインストーラブルトリガーとして設定が必要**。簡易トリガーとして自動では動かない。設定方法：GASエディタ→左サイドバーの時計アイコン（トリガー）→トリガーを追加→`onFormSubmit`を選択→イベントの種類「フォーム送信時」→スプレッドシートを選択。

### 主要なヘルパー関数

| 関数名 | 役割 |
|---|---|
| `normalizeToken(str)` | NFKC正規化＋全角英数→半角変換。表記揺れ吸収のために各所で使う |
| `extractTokens(str)` | 括弧内トークンと本体トークンをそれぞれ抽出。「saya(Rem)」→`['saya(rem)', 'rem', 'saya']`のような配列を返す |
| `checkArtistDuplicate(e)` | 作家マスタ編集時に重複チェック（`onEdit`から呼ばれる） |
| `buildProductSheet(sheetName, rows, headers)` | 全消去→ヘッダー（青背景）＋データを書き込む。交互背景・列幅自動調整・1行目固定付き。シートを返す |
| `markFormReceived(artistName, targetCols)` | 作家名をトークン照合でゆるく一致させてチェックを入れる共通処理。候補が複数あれば`ambiguous`を返して自動チェックしない |
| `findPeriodArtistCandidates(targetName, currentPeriod)` | 会期ごと作家情報の中から、指定会期内で名前のトークンが重なる行を探す |
| `getSquareSalesMap()` | Square売上シートを読んで `normalizeToken(作家名)+'|'+normalizeToken(作品名)` → 実質販売数（販売数－払い戻し数）のMapを返す |

---

## キーとなる設計思想

### 突き合わせキー
全シート共通で **`normalizeToken(作家名) + '|' + normalizeToken(作品名)`** をキーとして使う。SquareとのマッチングもSKUではなくこのキーで行う（SKUが空欄の商品が多いため）。

### Square側の運用ルール（第5回から確定）
- Square商品登録時：商品名＝作家名、商品バリエーション＝作品名（商品コード管理・商品一覧_確定と**完全に同じ表記でコピペ**すること）
- 商品番号（SKU）は入力してもよいが突き合わせには使わない

### 在庫数の考え方
- `在庫数（理論値）`＝ 納品済み＋買取済在庫＋繰越入庫－振替出庫－破損－返却－取置き中－紛失－サンプル販売不可分。**推定販売数（実売り）は引かない**
- `残在庫(実数)`＝会期末に実際に数えた数を商品コード管理「在庫残数」に手入力→在庫管理(商品ごと)がミラー表示
- `推定販売数`＝ 在庫数（理論値）－残在庫(実数)（手入力前は空欄）
- SKUごとには`推定販売数`とSquare由来の`Square販売数`を並べて突き合わせる
- 買取済在庫・繰越入庫・振替出庫は「今回作家から新しく届いた点数」ではないため`納品済み(サンプル込)`には含めない（詳細は「設計判断の背景」参照）
- 破損・返却は列としては出力されるが、通常は見る必要が薄いためデフォルトで非表示（折りたたみ）にしてある

### スクリプト実行でデータが消えない設計
- `buildProductSheet`は毎回全消去して再生成（destructive）なので、自動生成シートに手動入力してはいけない
- 会期末の在庫カウント（残在庫(実数)）の入力場所は**商品コード管理「在庫残数」列**。ここは手動更新されない列なので再実行で消えない

### 在庫変動ログの種別と集計対象外の検出
- 有効な種別：納品／サンプル／取り置き／取り置き解消／破損／返却／買取済在庫／紛失／繰越入庫／振替出庫
- 種別は完全一致で判定されるため、表記ゆれ（過去に「買取在庫」表記が使われていた等）があると**エラーも出ずに静かに集計から漏れる**。この事故を防ぐため、③在庫管理(商品ごと)を更新した際、種別が空欄または上記一覧にない行があれば完了メッセージに警告として表示するようにしてある

### 会期の管理
- 現在の会期は**ロイヤリティレポート!C13**のプルダウン値を参照する（スクリプト固定値なし）
- 候補は「会期ごと作家情報」A列のユニーク値。`setupRoyaltyPeriodDropdown()`でプルダウンを更新する
- 新しい会期が始まったら、まずこのプルダウンを更新して現在の会期を選択すること

### 別人同名の作家への対応
- `extractTokens`でトークン分解して部分一致でゆるくマッチング（例：「saya」と「saya(Rem)」は両方が「saya」を含むので候補に上がる）
- 同一会期内で候補が2件以上の場合は自動チェックせず、手動確認を促す

---

## 設計判断の背景（なぜそうしたか／なぜそうしないか）

会話の中で出てきた「なぜこの設計にしたか」を記録する節。コードだけを読んでも再現できない判断理由はここに追記していく。

### 納品確認書は在庫変動ログの「納品」種別を見ない
納品確認書は`商品一覧_確定`（納品確定フォームの回答）を直接見ており、在庫変動ログの「納品」実績とは連動していない。これは意図的な設計：納品確認書は作家に送付する書類であり、「作家が事前に申告した納品予定数」をそのまま反映する必要がある。在庫変動ログの実績ベースにしてしまうと、ログの記入漏れ・遅れがそのまま作家への送付物に影響してしまうため、あえて別ソースにしている。

### 価格の唯一のソースは商品一覧_確定（商品コード管理ではない）
在庫管理(商品ごと)は当初、商品コード管理D列（税込価格）を価格ソースにしていたが、商品コード管理D列は「②-2 納品確定を更新」を実行したときにしか商品一覧_確定と同期されない仕組みだった。「②-1 商品一覧_確定のみ更新」だけを実行した場合、商品コード管理の価格が古いまま残ってしまうリスクがあり、かつそのリスクに気づく手段がなかった。人間が実行順序を覚えておかなければならない設計は事故のもとと判断し、価格は商品一覧_確定から直接取得する形に変更した（商品コード管理からはSKU・在庫残数のみを取得）。

### 在庫変動ログは「実績を記録する」方式にした（在庫管理シートを直接編集しない）
以前は在庫管理シートを直接手入力で編集して在庫の一覧を作っていたが、記憶や作家からの納品書と数字が食い違ったときにスタッフ全員に聞いて回る手間が発生し、誤編集のリスクもあった。ログへの記入という形に変えることで、「いつ・誰が・何を」という変動の記録自体が残るようにし、事後の答え合わせができるようにしている。

### 買取済在庫・繰越入庫・振替出庫は「納品済み(サンプル込)」列に含めない
これらはいずれも「今回の会期で作家から新しく届いた点数」ではないため、納品済み列に混ぜると「作家が実際に何点搬入したか」という数字の意味が崩れる。在庫数（理論値）には反映しつつ、納品済み列とは切り離して集計している。

### 破損・返却は列として出力しているが、初期状態では非表示にしている
在庫管理(商品ごと)・(SKUごと)は本来「これから売れる在庫が何点あるか／何点売れたか」を把握するためのシートであり、破損・返却の内訳は普段の運用では見る機会が少ない。一方でロイヤリティレポートの「販売在庫数」計算に必要なため、列としては出力しつつ、視認性を保つためデフォルトで折りたたんで（非表示にして）いる。

---

## 未実装・今後の課題

- **商品コード管理「ラベル表示作家名」の自動反映**（設計確定済み）：作家マスタのC列「ラベル表記名」を作家名キーで引いてくる処理をupdateProductCodeSheetCoreに追加する必要がある。現在はコードに組み込まれていない可能性があるため確認すること
- **updateFinalList実行時の作家マスタ未登録チェック**：`checkArtistsAgainstMaster`関数は実装済みだが、`updateFinalList`から呼ばれているか要確認
- **ラベル印刷用エクスポート**：商品コード管理シートをラベルプリンターソフト用にエクスポートする手順は未整備

---

## 注意事項

- 納品確認書の作家情報（本名・住所・電話番号・振込先）は**作家マスタから取得**している。作家マスタの列構成が変わったら、`fillInvoice`関数内のインデックスを合わせて修正すること（G列=住所1、I列=本名、J列=電話番号、K列=振込先情報）
- 商品一覧_確定・在庫管理系シートは実行のたびに全消去されるので、これらのシートへの手動入力は次回実行時に消える
- `onFormSubmit`の自動チェックは、ロイヤリティレポート!C13に会期が設定されていないと動かない。会期設定を忘れると全員チェックが入らない
- 納品確認書は商品が13点以上ある場合、28行目の直前に自動で行を挿入してから記入する。挿入した行数は開発者メタデータ（キー：`invoiceExtraRows`）に記録し、別の作家に切り替えるたびに前回分をいったん削除→今回必要な分だけ再追加している。合計金額などの数式は「範囲の直前に行を挿入すると自動で範囲が広がる」というスプレッドシートの仕様に依存しているため、シート側の数式やレイアウトを変更する際はこの前提が崩れないか確認すること
- 在庫管理(SKUごと)は「商品コードあり」の行を商品コードでグルーピングし、「商品コードなし」の行はグルーピングせずそのまま末尾にまとめて出力する仕様。そのため同じ作家の商品が「コードあり」と「コードなし」に分かれている場合、その作家の行が2箇所に分断され、太線（作家の区切り線）の位置がずれる可能性がある
